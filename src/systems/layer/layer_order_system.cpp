#include "layer_order_system.hpp"

namespace layer
{
    namespace layer_order_system
    {
        int newZIndex{0}; // Global variable to hold the running Z-index value. 0 is the bottom, and higher values are on top.
    }
}